# Inbox

Capture anything here — don't process. Just dump items as bullets. Process them later via `/process-inbox`.

The `/capture` skill appends bullets here automatically. You can also drop items by hand if Obsidian is open.
