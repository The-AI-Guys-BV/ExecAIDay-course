# Projects

One MD per active project. Filename is the project slug (e.g., `q3-board-deck.md`). The `/new-project` skill scaffolds a project file here. The `/project <name>` skill drills into one.

See `references/gtd-vault-layout.md` for the project file format.
