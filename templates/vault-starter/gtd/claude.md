This folder is your GTD store. Plain markdown files, one per list.

- Use inline tags (no YAML frontmatter).
- Capture into `inbox.md` only — process via `/process-inbox` to move bullets into the right destination file.
- Schema and tag conventions: `references/gtd-vault-layout.md`.
