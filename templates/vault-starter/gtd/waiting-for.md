# Waiting For

Items you've delegated or are waiting on someone else for. Each bullet uses `@person:<name>` and `created:<date>`.

Read with `/waiting-for` or `/waiting-for stale` to surface items >7 days old.
