# Next Actions

Single-step actionable items. Each bullet ends with inline tags for context, energy, time, and (optionally) project.

Read with `/whats-next` (filtered by context) or `/available-now <minutes> in <context>`.

Tag reference: `references/gtd-vault-layout.md`.
