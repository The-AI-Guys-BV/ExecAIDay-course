# References — context

Knowledge base. Cross-linked notes via `[[wikilinks]]`. Flat or lightly nested by topic.

Default behavior:
- Treat content here as authoritative reference for facts I've already verified.
- New references go here; notes-in-progress go in [[Inbox]].
- Use `[[wikilinks]]` when linking from a daily note or project back to a reference.
